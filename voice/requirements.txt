# ChatRuka voice service (Pipecat + Gemini Live)
# Python 3.11+ required.
#
#   python -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
#
# Extras:
#   google  -> Gemini Live (google-genai) speech-to-speech
#   webrtc  -> SmallWebRTC transport (aiortc) for the browser
#   runner  -> Pipecat dev runner (FastAPI/uvicorn) serving POST /api/offer
pipecat-ai[google,webrtc,runner]>=1.3.0

# Calling back into the Next.js /api/voice-tools endpoint.
aiohttp>=3.9.0

# (Pulled transitively, pinned here for clarity.)
python-dotenv>=1.0.0
loguru>=0.7.0
